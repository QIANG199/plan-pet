{
  "name": "plan-pet-server",
  "private": true,
  "version": "0.1.0",
  "description": "PC relay for PlanPet (desktop quota panel + clawd pet)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test",
    "hooks:install": "node ../hooks/install.js",
    "hooks:status": "node ../hooks/install.js status"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "serialport": "^13.0.0"
  }
}
