# ESP32 桌面额度面板 · 设计方案

> 版本：v1.6 · 日期：2026-08-23 · 状态：**已定稿**
> 配套设计稿：[screen-layout.html](./screen-layout.html)（v11）、[settings-layout.html](./settings-layout.html)（v1）
> 词汇表：[CONTEXT.md](../../CONTEXT.md)

## 修订说明（v1.5 → v1.6）

新增设备端交互三件套，均不改变中转服务契约：

- **BOOT 键**（GPIO0，strapping）：短按换肤、双击进设置页、长按 ≥5 秒松手重启（松手确认高电平后才 `ESP.restart`）。
- **关机倒计时**：长按 PWR 3 秒 → 状态列预留位进度条 → 3·2·1 倒计时 → 断电；期间再按 PWR 取消；USB 插入永不关机（原"长按即黑屏"废除）。
- **设置页**：纯触屏配网（已否决 AP+手机网页方案）：WiFi 扫描列表 + 软键盘密码 + 主机/端口/令牌编辑，界面规格见 settings-layout.html。串口命令保留为开发备用通道。
- RESET 键接 EN 硬复位线，固件不可编程（明确否决"软件拦截 RST"）。
- 固件目录分层为 `src/ui/` 与 `src/bsp/`；hooks 事件表收敛到 `server/src/lib/events.js`。

## 修订说明（v1.4 → v1.5）

CURSOR 屏上标签定为 `composer & grok` / `other`（数据仍来自 `autoPercentUsed` / `apiPercentUsed`）。时间下分隔线加长至 52px。本版锁定，开发板到位后按此实现。

## 修订说明（v1.3 → v1.4）

屏上文案全英文：GLM `5h` / `7d`，CURSOR `auto` / `api`。百分比靠右；倒计时在百分比左侧。卡底留 8px。状态列时间横向居中，其下一条短分隔线，中部留空。快照字段 `cursorModels` / `otherModels` 改为 `auto` / `api`。失败态写 `fail`，不再做汉字子集。

## 修订说明（v1.2 → v1.3）

布局微调，显示字段不变。状态列去掉套餐日期与品牌名；重置时间全部写在对应额度卡上。桌宠区改为 160×160 正方形。进度条警告色改为 clawd 橙 `#f0805a`。电源用充电闪电 vs 电池填充区分。

## 修订说明（v1.1 → v1.2）

显示字段已拍板。只拉下列接口字段；屏上在精简稿基础上增加下次套餐/账期日期。

| 来源 | 取用字段 | 屏上 |
|---|---|---|
| Cursor `usage-summary` | `membershipType` | 入库，不写 Pro |
| | `billingCycleStart` / `billingCycleEnd` | CURSOR 标题右侧倒计时（用 End） |
| | `autoPercentUsed` / `apiPercentUsed` | 屏上 `composer & grok` / `other` |
| GLM `quota/limit` | `data.level` | 入库，不写 Pro |
| | `TOKENS_LIMIT.unit + number` | 3+5 = `5h`；6+7 = `7d` |
| | `TOKENS_LIMIT.percentage` | 条宽与数字 |
| | `TOKENS_LIMIT.nextResetTime` | 5h 行右侧短倒计时；周条右侧周倒计时 |

不拉、不显示：MCP `TIME_LIMIT`、on-demand 花费、`plan.used/limit`、按模型 events。

进度条颜色按**已用**百分比：低于 70% 绿，70–90% 橙，不低于 90% 红（四条同一规则）。

运行指示：去掉左上应用徽标；绿点按各应用是否在跑独立显示（可同时亮）；桌宠动画 `pet.state` 同时 busy 时以 GLM 为准。idle/sleeping 无点。

## 1. 项目概述

用微雪 ESP32-S3-Touch-LCD-3.49（A 款带锂电池，SKU=32373）开发一块桌面摆件面板，同时展示：

1. **GLM Coding Plan（国际版 Z.ai）额度**：`5h` 与 `7d` 窗口的已用百分比、行内倒计时
2. **Cursor Pro 额度**：`composer & grok` / `other` 已用百分比、账期结束倒计时
3. **clawd 桌宠**：随会话事件切换；绿点表示该应用在跑（可同时亮），桌宠动画跟 **当前代理**

开发者为 Java 后端背景、首次接触硬件开发，方案向「逻辑简单、易调试、文档完备」倾斜。额度面板是哑终端：不持有 Cursor 登录态，不实现业务规则。

## 2. 需求与已确认决策

| 项 | 决策 | 说明 |
|---|---|---|
| 架构 | PC 中转服务 + ESP32 纯显示端 | Node.js 聚合；ESP32 只渲染快照 |
| 中转服务技术栈 | Node.js | 内置 `fetch`/`http`；**不**复用 clawd 的 hook 文件 |
| Cursor 订阅 | Pro（$20/月） | 只显示两段百分比 + 账期结束日；不显示 on-demand |
| GLM 查询 | 官方 monitor API | `api.z.ai/api/monitor/usage/quota/limit`，API Key 认证 |
| ZCode 事件 | 官方 hooks | `~/.zcode/cli/config.json`，与 Claude Code 协议兼容 |
| Cursor 事件 | **自写适配器** | 把 Cursor camelCase 钩子归一成契约事件后 POST 本服务 |
| 桌宠形象 | clawd 主题（橙螃蟹） | 转帧输入：`D:\develop\clawd-on-desk\assets\gif` |
| 桌宠状态 | 6 态 | idle / thinking / typing / happy / error / sleeping |
| 主题 | 深浅双色 | **主路径**：触摸桌宠区。BOOT 仅开发调试（按键在机身背面） |
| 屏幕方向 | 横向摆放 | 物理 172×640，横屏画布 640×172（LVGL `ROT_90`） |
| 额度刷新 | 每 5 分钟（PC 拉外部接口） | 面板仍 2 秒轮询快照，倒计时在设备本地走 |
| 桌宠状态轮询 | 每 2 秒 GET `/api/dashboard` | 轮询而非 WebSocket |
| 发现 | mDNS 主机名 `desktop-pet.local` | 固件不写死 PC 的 DHCP IP |
| 配对 | 面板令牌 | 请求头 `X-Panel-Token`；首次烧录后串口写入 NVS |
| 供电 | USB 常亮 | 锂电池为临时移动；首次 WiFi 也走 USB 串口配置 |
| 时区 | Asia/Shanghai | 屏幕时钟；NTP 为 UTC，设备换算 |
| 开发工具 | Cursor / VS Code + PlatformIO | Arduino **框架**，不是 Arduino IDE |

## 3. 系统架构

```
┌──────────────── 你的电脑（Node.js 中转服务 :3737，mDNS desktop-pet.local）─┐
│  鉴权：X-Panel-Token；首次运行写 Windows 防火墙入站规则                      │
│                                                                          │
│  Cursor 适配器 ──┐                  ┌── Cursor 账单接口                    │
│  ZCode 适配器  ──┼─→ 事件接收/状态机 ─┤   (state.vscdb 读 token)              │
│                  │                  └── Z.ai monitor API (API Key)         │
│                  ↓                                                       │
│           聚合内存状态 ──→ GET /api/dashboard                               │
└─────────────────────────────────┬────────────────────────────────────────┘
                                  │ HTTP 轮询（WiFi，每 2s，带面板令牌）
                                  ↓
                         ┌─────────────────┐
                         │  ESP32-S3 面板   │
                         │  LVGL 渲染       │
                         │  额度卡+桌宠动画  │
                         └─────────────────┘
```

**数据流**：

- 事件流：编码工具触发 hooks → **本仓库适配器**（不是 clawd 的 `cursor-hook.js`）POST `/api/event` → 状态机更新桌宠
- 额度流：中转服务每 5 分钟拉 Cursor / Z.ai → 内存缓存
- 显示流：额度面板每 2 秒 GET `/api/dashboard`，一次拿全；`nextResetAt` 由设备本地倒计时
- 发现流：额度面板解析 `desktop-pet.local`，禁止把电脑 IP 当作唯一配置

## 4. 外部接口依赖

### 4.1 GLM Coding Plan（国际版）

- 端点：`GET https://api.z.ai/api/monitor/usage/quota/limit`
- 认证：`Authorization: <API_KEY>`（**无** `Bearer` 前缀，与 cc-switch #1588 一致）
- **字段映射（v1.0 写反，此处以 #1588 抽取逻辑为准）**：

| `limits[].type` | 判定 | 面板 |
|---|---|---|
| `TOKENS_LIMIT` 且 `unit=3, number=5` | 5 小时窗口 | 上条 % + 行内倒计时 |
| `TOKENS_LIMIT` 且 `unit=6, number=7` | 7 天窗口 | 下条 `7d` % + 行内倒计时；无此条则隐藏 |
| 其它 `TOKENS_LIMIT` | 按 `nextResetTime` 升序兜底 | 短窗 / 长窗 |
| `TIME_LIMIT` | MCP 月度次数 | **不做** |

只使用 `percentage` 与 `nextResetTime`。`data.level` 入库。不使用 `usage/currentValue/remaining`。

### 4.2 Cursor（非官方接口，社区验证方案）

- 认证：WorkOS 会话。本机验证有效写法是 Cookie `WorkosCursorSessionToken={cachedAuthId}::{accessToken}`（`adminSettings.cachedAuthId` + `cursorAuth/accessToken`）。只丢 accessToken 会 401
- **取用字段（已拍板）**：`membershipType`、`billingCycleStart`、`billingCycleEnd`、`plan.autoPercentUsed`、`plan.apiPercentUsed`
- 进度条只用两个 Percent。禁止用 `plan.used/limit` 当百分比
- CURSOR 标题右侧倒计时 = `billingCycleEnd`（本机 2026-09-21）
- 不取 on-demand、不取 aggregated events

### 4.3 ZCode hooks（官方机制）

- 配置：`~/.zcode/cli/config.json`，`hooks.enabled: true`；只用用户级，不用项目级
- 协议：stdin 一行 JSON（`hook_event_name`、`session_id`、`tool_name`），stdout 必须在约 800ms 内写出合法 JSON，POST 异步
- 监听：`UserPromptSubmit` / `PreToolUse` / `PostToolUse` / `PostToolUseFailure` / `Stop`
- 安装：本仓库脚本往用户级 hooks **追加**命令，不覆盖已有 clawd 条目

### 4.4 Cursor hooks（自写适配器，禁止原样复用 clawd）

clawd-on-desk 的 `hooks/cursor-hook.js` **不能**直接用：

- 它 POST 到 clawd 的 `23333–23337/state`，不是本服务 `:3737/api/event`
- 事件名是 camelCase，与契约不一致
- 它把 `postToolUseFailure` 映射成 working，本方案要 error
- 与 clawd 同时安装会争用 `~/.cursor/hooks.json`（应并存为多条命令）

本仓库适配器负责：

1. 800ms 内对 Cursor 写出合法 stdout JSON（`beforeSubmitPrompt` 回 `{ continue: true }`，其余 `{}`）
2. 把事件名归一成第 5.2 节契约后再 POST
3. 探测失败时静默丢事件，不得阻塞 IDE

| Cursor 钩子名 | 契约 `event` |
|---|---|
| `beforeSubmitPrompt` | `UserPromptSubmit` |
| `preToolUse` | `PreToolUse` |
| `postToolUse` | `PostToolUse` |
| `postToolUseFailure` | `PostToolUseFailure` |
| `stop` | `Stop`（payload.status 为 error 时仍报 `Stop`，由状态机看是否进 error） |
| `sessionStart` | `SessionStart`（仅用于把 sleeping 唤醒为 idle） |
| 其余 | 忽略 |

## 5. 中转服务 API 契约

监听 `0.0.0.0:3737`（额度面板在局域网，不能只绑 `127.0.0.1`）。mDNS 名 `desktop-pet.local`。除健康检查外均校验 `X-Panel-Token`。

### 5.1 `GET /api/dashboard`（额度面板轮询）

```json
{
  "ts": 1760760000,
  "cursor": {
    "ok": true,
    "level": "pro",
    "cycleStart": 1760755661,
    "cycleEnd": 1763434061,
    "auto": { "pct": 4 },
    "api":  { "pct": 0 }
  },
  "glm": {
    "ok": true,
    "level": "pro",
    "h5":   { "pct": 12, "nextResetAt": 1760767680 },
    "week": { "pct": 44, "nextResetAt": 1761280000 }
  },
  "pet": {
    "agent": "zcode",
    "state": "typing",
    "since": 1760759160,
    "dots": { "zcode": true, "cursor": true }
  }
}
```

约定：

- `ok=false` 时该卡显示 `fail`，另一张卡与桌宠继续
- Cursor 快照键 `auto` / `api` 对应屏上 `composer & grok` / `other`（来自 `autoPercentUsed` / `apiPercentUsed`）
- 老套餐无 7d 额度：`glm.week` 为 `null`，下条进度条不画
- `pet.agent ∈ {cursor, zcode, null}`：桌宠动画跟谁走（同时 busy 时为 zcode）
- `pet.state ∈ {idle, thinking, typing, happy, error, sleeping}`：桌宠动画
- `pet.dots.zcode` / `pet.dots.cursor`：对应额度卡绿点，可同时为 true
- 倒计时用 `nextResetAt` / `cycleEnd`（Unix 秒），设备本地递减
- 无 `resetMin`、无 `onDemand`、无 `includedUsd`

### 5.2 `POST /api/event`（适配器上报）

```json
{
  "source": "zcode",
  "event": "PostToolUse",
  "tool": "Write",
  "session": "session-123",
  "ts": 1760759160
}
```

- `source ∈ {cursor, zcode}`
- `event` 只接受上表契约名。Cursor 别名在适配器内消化，服务端不认 camelCase
- 无令牌 → 401；未知 event → 204 忽略

### 5.3 发现、配网、防火墙

- 中转服务启动时注册 mDNS：`desktop-pet.local`
- 首次运行尝试添加 Windows 防火墙 TCP 3737 入站规则（失败则日志提示手动放行）
- 额度面板只保存：WiFi SSID/密码、`desktop-pet.local`、面板令牌。**不保存电脑 IP 作为唯一地址**
- 首次配网：USB 烧录后串口写入（设备 USB 常亮，不另做 SoftAP）。改 WiFi 同样走串口
- 令牌：中转服务首次启动生成并打印；用同一串口命令写入 NVS

## 6. 桌宠状态机

状态机只在中转服务。额度面板只播 `pet.state` 对应帧。

| 契约事件 | 下一态 | 素材 |
|---|---|---|
| （启动 / 尚无事件） | idle | clawd-idle.gif |
| SessionStart（当前为 sleeping） | idle | clawd-idle.gif |
| UserPromptSubmit / PreToolUse | thinking | clawd-thinking.gif |
| PostToolUse | typing | clawd-typing.gif |
| Stop（非失败） | happy（保持 4s） | clawd-happy.gif |
| PostToolUseFailure，或 Stop 且失败 | error（保持 4s） | clawd-error.gif |
| happy / error 满 4s | idle | clawd-idle.gif |
| 15 分钟无任何事件 | sleeping | clawd-sleeping.gif |

规则：

- **Idle** 是清醒待机：一轮已结束或尚未调工具，但还不到入睡。不是 typing，也不是 sleeping
- typing **仅**在工具调用进行中（PreToolUse 之后、Stop 之前的 PostToolUse 段）
- Cursor 若漏发 Stop，最后一次 PostToolUse 后约 10s 无新工具则视为结束（happy → idle）
- happy / error 可被新的 UserPromptSubmit / PreToolUse **立即打断**；Stop 之后迟到的 PostToolUse 不打断 happy / idle
- 绿点两边独立，谁在 thinking/typing/happy/error 谁亮。桌宠动画同时 busy 时跟 GLM（zcode），避免被 Cursor 事件拽走
- 15 分钟计时器在每一次成功处理的 `/api/event` 上重置

## 7. UI 规格（详见设计稿）

- 物理分辨率：172 × 640（IPS，QSPI，AXS15231B，电容触摸）
- 横屏画布：640 × 172；驱动使用微雪例程的旋转宏（`USER_DISP_ROT_90`），触摸坐标同步旋转
- 布局：内边距 6px；状态列 **80×160** ｜ 间距 8 ｜ 额度列 **372** ｜ 间距 8 ｜ 桌宠 **160×160 正方形**。验算 6+80+8+372+8+160+6=640，高 6+160+6=172
- 状态列：时间（顶，**横向居中**）→ 其下短分隔线（52px，不拉满）→ 中部留空 → 底行 WiFi + 电源图标 + 百分比。充电用闪电电池，供电用填充电池。LVGL 用 `LV_SYMBOL_CHARGE` / `LV_SYMBOL_BATTERY_*`
- GLM 卡 372×78：`GLM`+绿点；行左 `5h` / `7d`；行右倒计时 + 百分比。卡 padding 6/10/8，末条距底 8px
- Cursor 卡 372×78：`CURSOR` + 标题右侧账期倒计时；行左 `composer & grok` / `other`；行右百分比。数据键仍为快照 `auto` / `api`
- 绿点：该侧 thinking/typing/happy/error 常亮，两边可同时亮；桌宠动画另按 GLM 优先选态
- 进度条高 7px。已用 `[0,70)` 绿 `#22c55e`；`[70,90)` clawd 橙 `#f0805a`；`[90,∞)` 红 `#ef4444`
- 桌宠区 160×160；帧按正方形画布居中（约 150×120 可视），8–12fps
- 主题：深色默认 / 浅色；点桌宠切换。BOOT 仅调试
- **真机字号门禁**：时间 ≥16px，卡标题 ≥12px，进度标签与倒计时 ≥11px
- 屏上无汉字；失败态 `fail`
- 设计稿：`docs/design/screen-layout.html` v11

## 8. 技术选型

| 层 | 选型 | 理由 |
|---|---|---|
| 构建 / 烧录 | PlatformIO（Arduino 框架） | 与 Cursor 同编辑器；补全、依赖、烧录一体。**不安装 Arduino IDE** |
| 厂商驱动 | 微雪 Demo 包 `Arduino/libraries` 拷进工程 | 官方未提供 PlatformIO 板型；板子按 ESP32-S3 + 16MB Flash + 8MB OPI PSRAM 配置 |
| 对照手段 | 官方 Arduino IDE 例程（可选） | 仅当 PlatformIO 点不亮时，用来区分「硬件坏了」还是 `platformio.ini` 错了 |
| UI | 微雪移植的 LVGL（跟例程版本，不混 8/9） | 卡片、进度条、字库 |
| 帧存储 | LittleFS | 深/浅两套 RGB565；6 态。帧数以 GIF 实测为准，不假定 12 |
| 动画 | 构建期 GIF 按主题背景合成后转 RGB565 | 吉祥物 GIF 带透明通道，RGB565 无 alpha；运行时不解码 GIF |
| 中转服务 | Node.js，少依赖 | 开机自启（Windows 任务计划或启动文件夹，阶段 6） |
| 时间 | NTP + PCF85063，时区 Asia/Shanghai | 断网后 RTC 仍走；倒计时用绝对时间戳 |

动画体积（上限估算）：单帧 160×160×2 ≈ 51KB；6 态 × 2 主题 × 12 帧 ≈ 7.4MB。超了先降帧数，不砍浅色主题。实际 GIF 按正方形画布居中裁切，不是拉满 160。

## 9. 关键设计决策（ADR 摘要）

1. **PC 中转而非 ESP32 直连**：Cursor token 在本机 SQLite；中转同时做事件聚合与额度缓存。代价：面板依赖电脑开机。
2. **轮询而非 WebSocket**：2s 轮询代码少、断线自愈；局域网流量可忽略。
3. **状态机在服务端**：固件保持哑终端。
4. **构建期按主题烤帧，而非运行时解 GIF / 合成 alpha**：换 Flash 换简单固件。透明通道不能烤死在单一背景上。
5. **Arduino 框架 + PlatformIO，而非 ESP-IDF，也而非 Arduino IDE**：跟微雪 Arduino 例程 API；日常开发留在 Cursor。
6. **mDNS 主机名而非写死 IP**：DHCP 变更不应导致重烧固件。
7. **自写 hooks 适配器，而非安装 clawd 的 cursor-hook.js**：端口、路径、事件名、失败态映射都不同；与 clawd 并存。

## 10. 风险与缓解

| 风险 | 等级 | 缓解 |
|---|---|---|
| Cursor 非官方接口变动 | 高 | 跟 2+ 开源实现；该卡 `fail`，不影响其余 |
| WorkOS token 过期 | 中 | 每次拉取重读 vscdb；失败提示重新登录 Cursor |
| 真机字号偏小 | 中 | 阶段 2 字号门禁；不达标减文案（周条、on-demand、倒计时择一） |
| PlatformIO 无官方板型 | 中 | 先拷微雪库 + 对齐 Flash/PSRAM/USB CDC；仍失败再用官方 Arduino 例程对照 |
| mDNS 在部分路由器/Windows 上失败 | 中 | 串口允许写入备用主机名或一次 IP，作发现失败的逃生口，不是默认路径 |
| WiFi 断连 | 低 | 图标置灰、指数退避；显示最后快照；倒计时本地继续 |
| 帧体积超 Flash | 中 | 6 态×双主题；构建脚本打印实测字节；超限降帧 |
| 钩子拖慢 IDE | 中 | 适配器 800ms 内先写 stdout，再异步 POST |
| 局域网伪造事件 | 低 | 面板令牌；家用仍建议开启 |

## 11. 开发阶段概览

设计已定稿。阶段 1 起建 `server/`；`firmware/`、`tools/` 随后。

| 阶段 | 内容 | 验收 |
|---|---|---|
| 0 环境搭建 | PlatformIO + 微雪 Arduino 库；横屏旋转 | 屏幕点亮 LVGL demo（Arduino IDE 非必须） |
| 1 中转服务 | GLM 映射（TOKENS_LIMIT 排序）+ 令牌 + mDNS + `/api/dashboard` | 浏览器带令牌访问，5h/7d 数字与 z.ai 网页一致，**不是** MCP 的 TIME_LIMIT |
| 2 联网渲染 | 串口写 WiFi/令牌；解析 `desktop-pet.local`；三列布局 | 面板用主机名拉到真实 GLM；字号门禁通过 |
| 3 桌宠动画 | 双主题烤帧 + 6 态切换 | idle/thinking/typing/happy/error/sleeping 可手动切；深浅两套 |
| 4 hooks | 自写 ZCode + Cursor 适配器，追加安装不覆盖 clawd | 编码时桌宠联动；clawd 若在运行仍可同时工作 |
| 5 Cursor 额度 | vscdb token + usage-summary；on-demand 显示规则 | 两段 Included；超额时看到花费 |
| 6 收尾 | 点桌宠切主题、断线重连、亮度、电池、开机自启、防火墙脚本 | 整机验收 |

> 任务拆解与排期：终稿确认后再写 `docs/plan.md`。

## 12. 参考链接

- 开发板：https://www.waveshare.net/wiki/ESP32-S3-Touch-LCD-3.49
- ZCode hooks：https://zcode.z.ai/docs/hooks
- GLM 用量映射：https://github.com/farion1231/cc-switch/issues/1588
- Cursor 用量参考：https://github.com/FreePeak/cursor-usage 、https://github.com/lixwen/cursor-usage-monitor
- 桌宠素材： [Clawd on Desk](https://github.com/rullerzhou-afk/clawd-on-desk) 的 `assets/gif/`（运行时 SVG 不进固件；贴图许可见其 [assets/LICENSE](https://github.com/rullerzhou-afk/clawd-on-desk/blob/main/assets/LICENSE)）
