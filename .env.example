# Z.ai Coding Plan API key (Authorization header, no Bearer)
ZAI_API_KEY=

# Shared with the ESP32 panel (header X-Panel-Token)
PANEL_TOKEN=

# Bind address. 0.0.0.0 so the panel can reach this PC on the LAN.
HOST=0.0.0.0
PORT=3737
